{
  "name": "charaktererstellung",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@material-ui/core": "^3.1.0",
    "axios": "^0.18.0",
    "env-cmd": "^8.0.2",
    "react": "^16.4.0",
    "react-bootstrap-sweetalert": "^4.4.1",
    "react-dom": "^16.4.0",
    "react-redux": "^5.1.0",
    "redux": "^4.0.1",
    "redux-thunk": "^2.3.0"
  },
  "scripts": {
    "start": "env-cmd development.env react-scripts start",
    "build": "react-scripts build",
    "build:dev": "env-cmd development.env npm run build",
    "build:test": "env-cmd test.env npm run build",
    "build:prod": "env-cmd production.env npm run build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "browserslist": [
    "defaults",
    "not dead"
  ],
  "devDependencies": {
    "dotenv": "^6.1.0",
    "lodash": "latest",
    "react-scripts": "^5.0.1",
    "redux-devtools-extension": "^2.13.5",
    "redux-logger": "^3.0.6",
    "webpack-bundle-analyzer": "^3.0.3"
  }
}
